[package]
name = "stage-1"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common/" , features = ["real_mode"]} 
badfs = { git = "https://github.com/spencer3035/badfs", branch = "main" }
